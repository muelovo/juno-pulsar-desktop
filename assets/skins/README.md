# 希望之心·晶翼

This original generated artwork was made using the three user-provided reference images for visual direction. The supplied game captures are not included. The skin package and source render are licensed CC BY 4.0 by the project contributors. The package contains only PNG sprite sheets and a manifest; it can be imported directly as `hope-heart-realistic-1.0.0.jpskin`.

Regenerate the sprite sheets with `python scripts/build-reference-skin.py` (requires Pillow). The generated package is written to `release/skins/`; copy it back here when updating the distributed skin.
