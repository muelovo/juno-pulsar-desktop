# M0 checkpoint

Commands (PowerShell): `npm ci`; `npm run build`; `cargo check --manifest-path src-tauri/Cargo.toml`; `npm run tauri dev`.

2026-09-22: TypeScript/Vite production build PASS; native Rust cargo check PASS (Windows x64, Rust 1.89). Native layer implements WorkerW discovery, timed shell message, SetParent, fallback bottom positioning, cursor polling and exact ListView->PIDL path probe.

Not yet validated: visible WorkerW composition on this Explorer version, actual desktop file hit, 2-hour soak, click/drag interaction, Explorer restart. Compile success does not establish these. Root sample images remain excluded. Next: M1 state machine and guarded recycle command, tray/settings, unit and manual tests.
