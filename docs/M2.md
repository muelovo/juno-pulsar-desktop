# M2 checkpoint

Run `npm run tauri dev`; choose local skin folder or .jpskin; choose a monitor or all monitors. Sample: `assets/sample-skin`. Skin spec: `docs/skins.md`.

2026-09-22: Rust 13 tests PASS (includes valid sample, extra executable refusal, invalid paths/IDs, bounds, APNG refusal); Vite build PASS. Per-monitor windows and physical coordinate conversion implemented; 2s monitor/Explorer signature watcher rebuilds layout and cancels proposals. PNG sprite sheets implemented and validated before installation/loading. No audio.

Not validated: mixed DPI actual capture, hot-plug while captured, successful Explorer recovery, multi-screen fullscreen detection. Only the primary companion per window is interactive; additional companions are visual. Skin capture remains a conservative 30px core scaled by app size, not alpha-based collision. Animated WebP/APNG and audio are intentionally refused. Next M3: bundling, CI, docs and release artifacts.
