# M1 checkpoint

Run: `npm run tauri dev`. Verify: `npm test`; `cargo test --manifest-path src-tauri/Cargo.toml`; `npm run build`.

2026-09-22: six frontend state tests PASS; six Rust tests PASS; production frontend build PASS. UI includes local settings, original procedural Canvas companion, confirmation, tray. Rust implements timed hover, single-use expiring proposal, file identity checks, protected-path policy, recycle progress sink, config and current-user autostart.

Input decision changed from M0: a dedicated-thread low-level mouse hook consumes only presses within a fresh sprite hit region over desktop HWND classes; the full overlay remains click-through. This avoids desktop drag-selection underneath the captured companion. Hook lifetime is RAII; Windows removes hooks on process termination. No code injection. Hook failure prevents startup instead of silently enabling unsafe interaction.

Still unverified: real pointer capture, safe recycle success/restore, UI and Explorer behavior, Windows 10, mixed DPI and hot plug. No existing user files were deleted by automated tests. Next M2: constrained skin format and import, monitor layout and lifecycle.
