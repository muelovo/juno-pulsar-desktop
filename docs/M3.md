# M3 checkpoint — 0.1.0 development preview

Commands: `npm run tauri build`; `./scripts/package-portable.ps1`. Run the portable executable without elevation. It stores configuration in AppData and requires an installed WebView2 Runtime.

2026-09-22 local results:
- TypeScript + Vite production build PASS.
- Vitest 6 tests PASS; Rust 20 tests PASS.
- Rustfmt, frontend Prettier check, Clippy all targets with warnings denied PASS.
- Native read-only Shell diagnostic PASS: ListView found, 37 items / 36 filesystem items. No filenames/paths printed.
- Native 8-second startup/exit smoke PASS. This host used bottom-window fallback because the expected WorkerW was not found.
- NSIS x64 bundle and portable ZIP generated, with SHA256SUMS.txt.
- GitHub repository configured: https://github.com/muelovo/juno-pulsar-desktop . Windows check/release workflows included.

Release is a **draft prerelease**, pending docs/manual-tests.md. No real user file was deleted during agent validation. Installer installation/uninstallation, real capture/recycle/restore, Windows 10, mixed DPI, hot plug and 2-hour performance are not marked passed. Additional companions are visual only; skin v1 is PNG sprite-sheet only; sound is absent. The existing local game reference images and audio files are excluded from Git and release packages.

Next: perform the manual acceptance matrix with disposable files, resolve compatibility findings, then publish the prerelease. No automated update or telemetry traffic is implemented.
